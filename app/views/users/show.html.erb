<br>
<center>
  <div class='col-sm-4 col-xs-4'>
    <div class="row">
      <div class="col">
        <div class="text-center">
          <%= mail_to @user.email, nil, :encode => :javascript%>
          <% if user_signed_in? && @user == current_user || current_user.admin? %>
            <hr>
            <%= link_to "Setting", edit_user_registration_path, class:"btn btn-warning"%>
          <%end%>
        </div>
      </div>
    </div>
  </div>
</center>
